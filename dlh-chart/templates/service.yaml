apiVersion: v1
kind: Service
metadata:
  name: {{ include "dlh-chart.fullname" . }}
  labels:
    {{- include "dlh-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dlh-chart.selectorLabels" . | nindent 4 }}
